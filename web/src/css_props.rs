//! items used in [`chunk`](crate::chunk!) macro intellisense.
//!
//! Requires the `css-types` feature.

#![allow(nonstandard_style, unused)]
/// The [`accent-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/accent-color) CSS property sets the accent color for user-interface controls generated by some elements.
pub static accent_color: () = ();
/// The CSS [`align-content`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/align-content) property sets the distribution of space between and around content items along a flexbox's cross axis, or a grid or block-level element's block axis.
pub static align_content: () = ();
/// The CSS [`align-items`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/align-items) property sets the `align-self` value on all direct children as a group.
pub static align_items: () = ();
/// The [`align-self`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/align-self) CSS property overrides a grid or flex item's `align-items` value.
pub static align_self: () = ();
/// The [`alignment-baseline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/alignment-baseline) CSS property specifies the specific baseline used to align the box's text and inline-level contents.
pub static alignment_baseline: () = ();
/// The [`all`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/all) shorthand CSS property resets all of an element's properties except `unicode-bidi`, `direction`, and CSS Custom Properties.
pub static all: () = ();
/// The [`anchor-name`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/anchor-name) CSS property enables defining an element as an anchor element by giving it one or more identifying anchor names.
pub static anchor_name: () = ();
/// The [`anchor-scope`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/anchor-scope) CSS property can be used to limit the scope in which a positioned element can be associated with anchor elements to a particular subtree.
pub static anchor_scope: () = ();
/// The [`animation`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation) shorthand CSS property applies an animation between styles.
pub static animation: () = ();
/// The [`animation-composition`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-composition) CSS property specifies the composite operation to use when multiple animations affect the same property simultaneously.
pub static animation_composition: () = ();
/// The [`animation-delay`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-delay) CSS property specifies the amount of time to wait from applying the animation to an element before beginning to perform the animation.
pub static animation_delay: () = ();
/// The [`animation-direction`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-direction) CSS property sets whether an animation should play forward, backward, or alternate back and forth between playing the sequence forward and backward.
pub static animation_direction: () = ();
/// The [`animation-duration`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-duration) CSS property sets the length of time that an animation takes to complete one cycle.
pub static animation_duration: () = ();
/// The [`animation-fill-mode`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-fill-mode) CSS property sets how a CSS animation applies styles to its target before and after its execution.
pub static animation_fill_mode: () = ();
/// The [`animation-iteration-count`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-iteration-count) CSS property sets the number of times an animation sequence should be played before stopping.
pub static animation_iteration_count: () = ();
/// The [`animation-name`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-name) CSS property specifies the names of one or more `@keyframes` at-rules that describe the animation to apply to an element.
pub static animation_name: () = ();
/// The [`animation-play-state`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-play-state) CSS property sets whether an animation is running or paused.
pub static animation_play_state: () = ();
/// The [`animation-range`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-range) CSS shorthand property is used to set the start and end of an animation's attachment range along its timeline, i.e., where along the timeline an animation will start and end.
pub static animation_range: () = ();
/// The [`animation-range-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-range-end) CSS property sets the point on the timeline where an animation should end.
pub static animation_range_end: () = ();
/// The [`animation-range-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-range-start) CSS property sets the point on the timeline where an animation should start.
pub static animation_range_start: () = ();
/// The [`animation-timeline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-timeline) CSS property specifies the timeline used to control the progress of a CSS animation.
pub static animation_timeline: () = ();
/// The [`animation-timing-function`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/animation-timing-function) CSS property sets how an animation progresses through the duration of each cycle.
pub static animation_timing_function: () = ();
/// The [`appearance`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/appearance) CSS property specifies the rendered appearance of replaced UI widget elements such as form controls.
pub static appearance: () = ();
/// The [`aspect-ratio`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/aspect-ratio) CSS property allows you to define the desired width-to-height ratio of an element's box.
pub static aspect_ratio: () = ();
/// The [`backdrop-filter`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/backdrop-filter) CSS property lets you apply graphical effects such as blurring or color shifting to the area behind an element.
pub static backdrop_filter: () = ();
/// The [`backface-visibility`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/backface-visibility) CSS property sets whether the back face of an element is visible when turned towards the user.
pub static backface_visibility: () = ();
/// The [`background`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background) shorthand CSS property sets all background style properties at once, such as color, image, origin, size, and repeat method.
pub static background: () = ();
/// The [`background-attachment`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-attachment) CSS property sets whether a background image's position is fixed within the viewport, or scrolls with its containing block.
pub static background_attachment: () = ();
/// The [`background-blend-mode`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-blend-mode) CSS property sets how an element's background images should blend with each other and with the element's background color.
pub static background_blend_mode: () = ();
/// The [`background-clip`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-clip) CSS property sets whether an element's background extends underneath its border box, padding box, or content box.
pub static background_clip: () = ();
/// The [`background-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-color) CSS property sets the background color of an element.
pub static background_color: () = ();
/// The [`background-image`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-image) CSS property sets one or more background images on an element.
pub static background_image: () = ();
/// The [`background-origin`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-origin) CSS property sets the background's origin: from the border start, inside the border, or inside the padding.
pub static background_origin: () = ();
/// The [`background-position`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-position) CSS property sets the initial position for each background image.
pub static background_position: () = ();
/// The [`background-position-x`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-position-x) CSS property sets the initial horizontal position for each background image.
pub static background_position_x: () = ();
/// The [`background-position-y`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-position-y) CSS property sets the initial vertical position for each background image.
pub static background_position_y: () = ();
/// The [`background-repeat`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-repeat) CSS property sets how background images are repeated.
pub static background_repeat: () = ();
/// The [`background-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/background-size) CSS property sets the size of the element's background image.
pub static background_size: () = ();
/// The [`baseline-shift`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/baseline-shift) CSS property repositions the dominant-baseline of a text element relative to the dominant-baseline of its parent text content element.
pub static baseline_shift: () = ();
/// The [`baseline-source`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/baseline-source) CSS property defines which baseline to use when inline-level boxes have multiple possible baselines, such as multi-line inline blocks or inline flex containers.
pub static baseline_source: () = ();
/// The [`block-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/block-size) CSS property defines the size of an element's block along the block axis.
pub static block_size: () = ();
/// The [`border`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border) shorthand CSS property sets an element's border.
pub static border: () = ();
/// The [`border-block`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block) CSS property is a shorthand property for setting the individual logical block border property values in a single place in the style sheet.
pub static border_block: () = ();
/// The [`border-block-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-color) CSS property defines the color of the logical block borders of an element, which maps to a physical border color depending on the element's writing mode, directionality, and text orientation.
pub static border_block_color: () = ();
/// The [`border-block-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-end) CSS property is a shorthand property for setting the individual logical block-end border property values in a single place in the style sheet.
pub static border_block_end: () = ();
/// The [`border-block-end-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-end-color) CSS property defines the color of the logical block-end border of an element, which maps to a physical border color depending on the element's writing mode, directionality, and text orientation.
pub static border_block_end_color: () = ();
/// The [`border-block-end-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-end-style) CSS property defines the style of the logical block-end border of an element, which maps to a physical border style depending on the element's writing mode, directionality, and text orientation.
pub static border_block_end_style: () = ();
/// The [`border-block-end-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-end-width) CSS property defines the width of the logical block-end border of an element, which maps to a physical border width depending on the element's writing mode, directionality, and text orientation.
pub static border_block_end_width: () = ();
/// The [`border-block-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-start) CSS property is a shorthand property for setting the individual logical block-start border property values in a single place in the style sheet.
pub static border_block_start: () = ();
/// The [`border-block-start-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-start-color) CSS property defines the color of the logical block-start border of an element, which maps to a physical border color depending on the element's writing mode, directionality, and text orientation.
pub static border_block_start_color: () = ();
/// The [`border-block-start-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-start-style) CSS property defines the style of the logical block start border of an element, which maps to a physical border style depending on the element's writing mode, directionality, and text orientation.
pub static border_block_start_style: () = ();
/// The [`border-block-start-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-start-width) CSS property defines the width of the logical block-start border of an element, which maps to a physical border width depending on the element's writing mode, directionality, and text orientation.
pub static border_block_start_width: () = ();
/// The [`border-block-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-style) CSS property defines the style of the logical block borders of an element, which maps to a physical border style depending on the element's writing mode, directionality, and text orientation.
pub static border_block_style: () = ();
/// The [`border-block-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-block-width) CSS property defines the width of the logical block borders of an element, which maps to a physical border width depending on the element's writing mode, directionality, and text orientation.
pub static border_block_width: () = ();
/// The [`border-bottom`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-bottom) shorthand CSS property sets an element's bottom border.
pub static border_bottom: () = ();
/// The [`border-bottom-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-bottom-color) CSS property sets the color of an element's bottom border.
pub static border_bottom_color: () = ();
/// The [`border-bottom-left-radius`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-bottom-left-radius) CSS property rounds the bottom-left corner of an element by specifying the radius (or the radius of the semi-major and semi-minor axes) of the ellipse defining the curvature of the corner.
pub static border_bottom_left_radius: () = ();
/// The [`border-bottom-right-radius`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-bottom-right-radius) CSS property rounds the bottom-right corner of an element by specifying the radius (or the radius of the semi-major and semi-minor axes) of the ellipse defining the curvature of the corner.
pub static border_bottom_right_radius: () = ();
/// The [`border-bottom-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-bottom-style) CSS property sets the line style of an element's bottom `border`.
pub static border_bottom_style: () = ();
/// The [`border-bottom-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-bottom-width) CSS property sets the width of the bottom border of an element.
pub static border_bottom_width: () = ();
/// The [`border-collapse`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-collapse) CSS property sets whether cells inside a `<table>` have shared or separate borders.
pub static border_collapse: () = ();
/// The [`border-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-color) shorthand CSS property sets the color of an element's border.
pub static border_color: () = ();
/// The [`border-end-end-radius`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-end-end-radius) CSS property defines a logical border radius on an element, which maps to a physical border radius that depends on the element's `writing-mode`, `direction`, and `text-orientation`.
pub static border_end_end_radius: () = ();
/// The [`border-end-start-radius`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-end-start-radius) CSS property defines a logical border radius on an element, which maps to a physical border radius depending on the element's `writing-mode`, `direction`, and `text-orientation`.
pub static border_end_start_radius: () = ();
/// The [`border-image`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-image) CSS property draws an image around a given element.
pub static border_image: () = ();
/// The [`border-image-outset`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-image-outset) CSS property sets the distance by which an element's border image is set out from its border box.
pub static border_image_outset: () = ();
/// The [`border-image-repeat`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-image-repeat) CSS property defines how the images for the sides and the middle part of the border image are scaled and tiled.
pub static border_image_repeat: () = ();
/// The [`border-image-slice`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-image-slice) CSS property divides the image specified by `border-image-source` into regions.
pub static border_image_slice: () = ();
/// The [`border-image-source`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-image-source) CSS property sets the source image used to create an element's border image.
pub static border_image_source: () = ();
/// The [`border-image-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-image-width) CSS property sets the width of an element's border image.
pub static border_image_width: () = ();
/// The [`border-inline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline) CSS property is a shorthand property for setting the individual logical inline border property values in a single place in the style sheet.
pub static border_inline: () = ();
/// The [`border-inline-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-color) CSS property defines the color of the logical inline borders of an element, which maps to a physical border color depending on the element's writing mode, directionality, and text orientation.
pub static border_inline_color: () = ();
/// The [`border-inline-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-end) CSS property is a shorthand property for setting the individual logical inline-end border property values in a single place in the style sheet.
pub static border_inline_end: () = ();
/// The [`border-inline-end-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-end-color) CSS property defines the color of the logical inline-end border of an element, which maps to a physical border color depending on the element's writing mode, directionality, and text orientation.
pub static border_inline_end_color: () = ();
/// The [`border-inline-end-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-end-style) CSS property defines the style of the logical inline end border of an element, which maps to a physical border style depending on the element's writing mode, directionality, and text orientation.
pub static border_inline_end_style: () = ();
/// The [`border-inline-end-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-end-width) CSS property defines the width of the logical inline-end border of an element, which maps to a physical border width depending on the element's writing mode, directionality, and text orientation.
pub static border_inline_end_width: () = ();
/// The [`border-inline-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-start) CSS property is a shorthand property for setting the individual logical inline-start border property values in a single place in the style sheet.
pub static border_inline_start: () = ();
/// The [`border-inline-start-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-start-color) CSS property defines the color of the logical inline start border of an element, which maps to a physical border color depending on the element's writing mode, directionality, and text orientation.
pub static border_inline_start_color: () = ();
/// The [`border-inline-start-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-start-style) CSS property defines the style of the logical inline start border of an element, which maps to a physical border style depending on the element's writing mode, directionality, and text orientation.
pub static border_inline_start_style: () = ();
/// The [`border-inline-start-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-start-width) CSS property defines the width of the logical inline-start border of an element, which maps to a physical border width depending on the element's writing mode, directionality, and text orientation.
pub static border_inline_start_width: () = ();
/// The [`border-inline-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-style) CSS property defines the style of the logical inline borders of an element, which maps to a physical border style depending on the element's writing mode, directionality, and text orientation.
pub static border_inline_style: () = ();
/// The [`border-inline-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-inline-width) CSS property defines the width of the logical inline borders of an element, which maps to a physical border width depending on the element's writing mode, directionality, and text orientation.
pub static border_inline_width: () = ();
/// The [`border-left`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-left) shorthand CSS property sets all the properties of an element's left border.
pub static border_left: () = ();
/// The [`border-left-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-left-color) CSS property sets the color of an element's left border.
pub static border_left_color: () = ();
/// The [`border-left-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-left-style) CSS property sets the line style of an element's left `border`.
pub static border_left_style: () = ();
/// The [`border-left-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-left-width) CSS property sets the width of the left border of an element.
pub static border_left_width: () = ();
/// The [`border-radius`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-radius) CSS property rounds the corners of an element's outer border edge.
pub static border_radius: () = ();
/// The [`border-right`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-right) shorthand CSS property sets all the properties of an element's right border.
pub static border_right: () = ();
/// The [`border-right-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-right-color) CSS property sets the color of an element's right border.
pub static border_right_color: () = ();
/// The [`border-right-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-right-style) CSS property sets the line style of an element's right `border`.
pub static border_right_style: () = ();
/// The [`border-right-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-right-width) CSS property sets the width of the right border of an element.
pub static border_right_width: () = ();
/// The [`border-spacing`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-spacing) CSS property sets the distance between the borders of adjacent cells in a `<table>`.
pub static border_spacing: () = ();
/// The [`border-start-end-radius`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-start-end-radius) CSS property defines a logical border radius on an element, which maps to a physical border radius depending on the element's `writing-mode`, `direction`, and `text-orientation`.
pub static border_start_end_radius: () = ();
/// The [`border-start-start-radius`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-start-start-radius) CSS property defines a logical border radius on an element, which maps to a physical border radius that depends on the element's `writing-mode`, `direction`, and `text-orientation`.
pub static border_start_start_radius: () = ();
/// The [`border-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-style) shorthand CSS property sets the line style for all four sides of an element's border.
pub static border_style: () = ();
/// The [`border-top`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-top) shorthand CSS property sets all the properties of an element's top border.
pub static border_top: () = ();
/// The [`border-top-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-top-color) CSS property sets the color of an element's top border.
pub static border_top_color: () = ();
/// The [`border-top-left-radius`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-top-left-radius) CSS property rounds the top-left corner of an element by specifying the radius (or the radius of the semi-major and semi-minor axes) of the ellipse defining the curvature of the corner.
pub static border_top_left_radius: () = ();
/// The [`border-top-right-radius`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-top-right-radius) CSS property rounds the top-right corner of an element by specifying the radius (or the radius of the semi-major and semi-minor axes) of the ellipse defining the curvature of the corner.
pub static border_top_right_radius: () = ();
/// The [`border-top-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-top-style) CSS property sets the line style of an element's top `border`.
pub static border_top_style: () = ();
/// The [`border-top-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-top-width) CSS property sets the width of the top border of an element.
pub static border_top_width: () = ();
/// The [`border-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-width) shorthand CSS property sets the width of an element's border.
pub static border_width: () = ();
/// The [`bottom`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/bottom) CSS property participates in setting the vertical position of a positioned element.
pub static bottom: () = ();
/// The [`box-decoration-break`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/box-decoration-break) CSS property specifies how an element's fragments should be rendered when broken across multiple lines, columns, or pages.
pub static box_decoration_break: () = ();
/// The [`box-shadow`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/box-shadow) CSS property adds shadow effects around an element's frame.
pub static box_shadow: () = ();
/// The [`box-sizing`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/box-sizing) CSS property sets how the total width and height of an element is calculated.
pub static box_sizing: () = ();
/// The [`break-after`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/break-after) CSS property sets how page, column, or region breaks should behave after a generated box.
pub static break_after: () = ();
/// The [`break-before`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/break-before) CSS property sets how page, column, or region breaks should behave before a generated box.
pub static break_before: () = ();
/// The [`break-inside`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/break-inside) CSS property sets how page, column, or region breaks should behave inside a generated box.
pub static break_inside: () = ();
/// The [`caption-side`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/caption-side) CSS property puts the content of a table's `<caption>` on the specified side.
pub static caption_side: () = ();
/// The [`caret-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/caret-color) CSS property sets the color of the insertion caret, sometimes referred to as the text input cursor.
pub static caret_color: () = ();
/// The [`clear`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/clear) CSS property sets whether an element must be moved below (cleared) floating elements that precede it.
pub static clear: () = ();
/// The [`clip-path`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/clip-path) CSS property creates a clipping region that sets what part of an element should be shown.
pub static clip_path: () = ();
/// The [`clip-rule`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/clip-rule) CSS property determines, when parts of the path overlap other parts, which pixels in a mask's box are inside the clipping shape defined by a clip path and which are outside.
pub static clip_rule: () = ();
/// The [`color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/color) CSS property sets the foreground color value of an element's text and text decorations, and sets the `currentColor` value.
pub static color: () = ();
/// The [`color-scheme`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/color-scheme) CSS property allows an element to indicate which color schemes it can comfortably be rendered in.
pub static color_scheme: () = ();
/// The [`column-count`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/column-count) CSS property breaks an element's content into the specified number of columns.
pub static column_count: () = ();
/// The [`column-fill`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/column-fill) CSS property controls how an element's contents are balanced when broken into columns.
pub static column_fill: () = ();
/// The [`column-gap`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/column-gap) CSS property sets the size of the gap (gutter) between an element's columns.
pub static column_gap: () = ();
/// The [`column-rule`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/column-rule) shorthand CSS property sets the width, style, and color of the line drawn between columns in a multi-column layout.
pub static column_rule: () = ();
/// The [`column-rule-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/column-rule-color) CSS property sets the color of the line drawn between columns in a multi-column layout.
pub static column_rule_color: () = ();
/// The [`column-rule-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/column-rule-style) CSS property sets the style of the line drawn between columns in a multi-column layout.
pub static column_rule_style: () = ();
/// The [`column-rule-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/column-rule-width) CSS property sets the width of the line drawn between columns in a multi-column layout.
pub static column_rule_width: () = ();
/// The [`column-span`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/column-span) CSS property makes it possible for an element to span across all columns when its value is set to `all`.
pub static column_span: () = ();
/// The [`column-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/column-width) CSS property sets the ideal column width in a multi-column layout.
pub static column_width: () = ();
/// The [`columns`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/columns) CSS shorthand property sets the maximum number of columns to use when drawing an element's contents, along with the minimum width and maximum height of the element's columns.
pub static columns: () = ();
/// The [`contain`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/contain) CSS property indicates that an element and its contents are, as much as possible, independent from the rest of the document tree.
pub static contain: () = ();
/// The [`contain-intrinsic-block-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/contain-intrinsic-block-size) CSS logical property defines the block size of an element that a browser can use for layout when the element is subject to size containment.
pub static contain_intrinsic_block_size: () = ();
/// The [`contain-intrinsic-height`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/contain-intrinsic-height) CSS property sets the height of an element that a browser can use for layout when the element is subject to size containment.
pub static contain_intrinsic_height: () = ();
/// The [`contain-intrinsic-inline-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/contain-intrinsic-inline-size) CSS logical property defines the inline-size of an element that a browser can use for layout when the element is subject to size containment.
pub static contain_intrinsic_inline_size: () = ();
/// The [`contain-intrinsic-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/contain-intrinsic-size) CSS shorthand property sets the size of an element that a browser will use for layout when the element is subject to size containment.
pub static contain_intrinsic_size: () = ();
/// The [`contain-intrinsic-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/contain-intrinsic-width) CSS property sets the width of an element that a browser will use for layout when the element is subject to size containment.
pub static contain_intrinsic_width: () = ();
/// The container shorthand CSS property establishes the element as a query container and specifies the name and type of the containment context used in a container query.
pub static container: () = ();
/// The container-name CSS property specifies a list of query container names used by the @container at-rule in a container query.
pub static container_name: () = ();
/// An element can be established as a query container using the [`container-type`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/container-type) CSS property.
pub static container_type: () = ();
/// The [`content`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/content) CSS property replaces content with a generated value.
pub static content: () = ();
/// The [`content-visibility`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/content-visibility) CSS property controls whether or not an element renders its contents at all, along with forcing a strong set of containments, allowing user agents to potentially omit large swathes of layout and rendering work until it becomes needed.
pub static content_visibility: () = ();
/// The [`counter-increment`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/counter-increment) CSS property can be used to increase or decrease the value of the named CSS counters by the specified values, or to prevent all counters or an individual counter's value from being changed.
pub static counter_increment: () = ();
/// The [`counter-reset`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/counter-reset) CSS property creates named CSS counters and initializes them to a specific value.
pub static counter_reset: () = ();
/// The [`counter-set`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/counter-set) CSS property sets CSS counters on the element to the given values.
pub static counter_set: () = ();
/// The [`cursor`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/cursor) CSS property sets the mouse cursor, if any, to show when the mouse pointer is over an element.
pub static cursor: () = ();
/// The [`direction`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/direction) CSS property sets the direction of text, table and grid columns, and horizontal overflow
pub static direction: () = ();
/// The [`display`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/display) CSS property sets whether an element is treated as a block or inline box and the layout used for its children, such as flow layout, grid or flex.
pub static display: () = ();
/// The [`dominant-baseline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/dominant-baseline) CSS property specifies the specific baseline used to align the box's text and inline-level contents.
pub static dominant_baseline: () = ();
/// The [`dynamic-range-limit`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/dynamic-range-limit) CSS property specifies the maximum luminance allowed for High Dynamic Range (HDR) content.
pub static dynamic_range_limit: () = ();
/// The [`empty-cells`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/empty-cells) CSS property sets whether borders and backgrounds appear around `<table>` cells that have no visible content.
pub static empty_cells: () = ();
/// The [`field-sizing`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/field-sizing) CSS property enables you to control the sizing behavior of elements that are given a default preferred size, such as form control elements.
pub static field_sizing: () = ();
/// The [`filter`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/filter) CSS property applies graphical effects like blur or color shift to an element.
pub static filter: () = ();
/// The [`flex`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flex) CSS shorthand property sets how a flex item will grow or shrink to fit the space available in its flex container.
pub static flex: () = ();
/// The [`flex-basis`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flex-basis) CSS property sets the initial main size of a flex item.
pub static flex_basis: () = ();
/// The [`flex-direction`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flex-direction) CSS property sets how flex items are placed in the flex container defining the main axis and the direction (normal or reversed).
pub static flex_direction: () = ();
/// The [`flex-flow`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flex-flow) CSS shorthand property specifies the direction of a flex container, as well as its wrapping behavior.
pub static flex_flow: () = ();
/// The [`flex-grow`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flex-grow) CSS property sets the flex grow factor, which specifies how much of the flex container's positive free space, if any, should be assigned to the flex item's main size.
pub static flex_grow: () = ();
/// The [`flex-shrink`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flex-shrink) CSS property sets the flex shrink factor of a flex item.
pub static flex_shrink: () = ();
/// The [`flex-wrap`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flex-wrap) CSS property sets whether flex items are forced onto one line or can wrap onto multiple lines.
pub static flex_wrap: () = ();
/// The [`float`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/float) CSS property places an element on the left or right side of its container, allowing text and inline elements to wrap around it.
pub static float: () = ();
/// The [`flood-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flood-color) CSS property defines the color of the current filter primitive subregion in `<feFlood>` and `<feDropShadow>` elements within a `<filter>`.
pub static flood_color: () = ();
/// The [`flood-opacity`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/flood-opacity) CSS property defines the opacity of the current filter primitive subregion in `<feFlood>` and `<feDropShadow>` elements within a `<filter>`.
pub static flood_opacity: () = ();
/// The [`font`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font) CSS shorthand property sets all the different properties of an element's font.
pub static font: () = ();
/// The [`font-family`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-family) CSS property specifies a prioritized list of one or more font family names and/or generic family names for the selected element.
pub static font_family: () = ();
/// The [`font-feature-settings`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-feature-settings) CSS property controls advanced typographic features in OpenType fonts.
pub static font_feature_settings: () = ();
/// The [`font-kerning`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-kerning) CSS property sets the use of the kerning information stored in a font.
pub static font_kerning: () = ();
/// The [`font-language-override`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-language-override) CSS property controls the use of language-specific glyphs in a typeface.
pub static font_language_override: () = ();
/// The [`font-optical-sizing`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-optical-sizing) CSS property sets whether text rendering is optimized for viewing at different sizes.
pub static font_optical_sizing: () = ();
/// The [`font-palette`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-palette) CSS property allows specifying one of the many palettes contained in a color font that a user agent may use for the font.
pub static font_palette: () = ();
/// The [`font-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-size) CSS property sets the size of the font.
pub static font_size: () = ();
/// The [`font-size-adjust`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-size-adjust) CSS property provides a way to modify the size of lowercase letters relative to the size of uppercase letters, which defines the overall `font-size`.
pub static font_size_adjust: () = ();
/// The [`font-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-style) CSS property sets whether a font should be styled with a normal, italic, or oblique face from its `font-family`.
pub static font_style: () = ();
/// The [`font-synthesis`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-synthesis) shorthand CSS property lets you specify whether or not the browser may synthesize the bold, italic, small-caps, and/or subscript and superscript typefaces when they are missing in the specified font-family.
pub static font_synthesis: () = ();
/// The [`font-synthesis-small-caps`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-synthesis-small-caps) CSS property lets you specify whether or not the browser may synthesize small-caps typeface when it is missing in a font family.
pub static font_synthesis_small_caps: () = ();
/// The [`font-synthesis-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-synthesis-style) CSS property lets you specify whether or not the browser may synthesize the oblique typeface when it is missing in a font family.
pub static font_synthesis_style: () = ();
/// The [`font-synthesis-weight`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-synthesis-weight) CSS property lets you specify whether or not the browser may synthesize the bold typeface when it is missing in a font family.
pub static font_synthesis_weight: () = ();
/// The [`font-variant`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-variant) CSS shorthand property allows you to set all the font variants for a font.
pub static font_variant: () = ();
/// The [`font-variant-alternates`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-variant-alternates) CSS property controls the usage of alternate glyphs.
pub static font_variant_alternates: () = ();
/// The [`font-variant-caps`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-variant-caps) CSS property controls the use of alternate glyphs used for small or petite capitals or for titling.
pub static font_variant_caps: () = ();
/// The [`font-variant-east-asian`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-variant-east-asian) CSS property controls the use of alternate glyphs for East Asian scripts, like Japanese and Chinese.
pub static font_variant_east_asian: () = ();
/// The [`font-variant-emoji`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-variant-emoji) CSS property specifies the default presentation style for displaying emojis.
pub static font_variant_emoji: () = ();
/// The [`font-variant-ligatures`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-variant-ligatures) CSS property controls which ligatures and contextual forms are used in the textual content of the elements it applies to.
pub static font_variant_ligatures: () = ();
/// The [`font-variant-numeric`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-variant-numeric) CSS property controls the usage of alternate glyphs for numbers, fractions, and ordinal markers.
pub static font_variant_numeric: () = ();
/// The [`font-variant-position`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-variant-position) CSS property controls the use of alternate, smaller glyphs that are positioned as superscript or subscript.
pub static font_variant_position: () = ();
/// The [`font-variation-settings`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-variation-settings) CSS property provides low-level control over variable font characteristics by letting you specify the four letter axis names of the characteristics you want to vary along with their values.
pub static font_variation_settings: () = ();
/// The [`font-weight`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font-weight) CSS property sets the weight (or boldness) of the font.
pub static font_weight: () = ();
/// The [`forced-color-adjust`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/forced-color-adjust) CSS property allows authors to opt certain elements out of forced colors mode.
pub static forced_color_adjust: () = ();
/// The [`gap`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/gap) CSS shorthand property sets the gaps (also called gutters) between rows and columns.
pub static gap: () = ();
/// The [`grid`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid) CSS property is a shorthand property that sets all of the explicit and implicit grid properties in a single declaration.
pub static grid: () = ();
/// The [`grid-area`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-area) CSS shorthand property specifies a grid item's size and location within a grid by contributing a line, a span, or nothing (automatic) to its grid placement, thereby specifying the edges of its grid area.
pub static grid_area: () = ();
/// The [`grid-auto-columns`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-auto-columns) CSS property specifies the size of an implicitly-created grid column track or pattern of tracks.
pub static grid_auto_columns: () = ();
/// The [`grid-auto-flow`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-auto-flow) CSS property controls how the auto-placement algorithm works, specifying exactly how auto-placed items get flowed into the grid.
pub static grid_auto_flow: () = ();
/// The [`grid-auto-rows`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-auto-rows) CSS property specifies the size of an implicitly-created grid row track or pattern of tracks.
pub static grid_auto_rows: () = ();
/// The [`grid-column`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-column) CSS shorthand property specifies a grid item's size and location within a grid column by contributing a line, a span, or nothing (automatic) to its grid placement, thereby specifying the inline-start and inline-end edge of its grid area.
pub static grid_column: () = ();
/// The [`grid-column-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-column-end) CSS property specifies a grid item's end position within the grid column by contributing a line, a span, or nothing (automatic) to its grid placement, thereby specifying the block-end edge of its grid area.
pub static grid_column_end: () = ();
/// The [`grid-column-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-column-start) CSS property specifies a grid item's start position within the grid column by contributing a line, a span, or nothing (automatic) to its grid placement.
pub static grid_column_start: () = ();
/// The [`grid-row`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-row) CSS shorthand property specifies a grid item's size and location within a grid row by contributing a line, a span, or nothing (automatic) to its grid placement, thereby specifying the inline-start and inline-end edge of its grid area.
pub static grid_row: () = ();
/// The [`grid-row-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-row-end) CSS property specifies a grid item's end position within the grid row by contributing a line, a span, or nothing (automatic) to its grid placement, thereby specifying the inline-end edge of its grid area.
pub static grid_row_end: () = ();
/// The [`grid-row-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-row-start) CSS property specifies a grid item's start position within the grid row by contributing a line, a span, or nothing (automatic) to its grid placement, thereby specifying the inline-start edge of its grid area.
pub static grid_row_start: () = ();
/// The [`grid-template`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-template) CSS property is a shorthand property for defining grid columns, grid rows, and grid areas.
pub static grid_template: () = ();
/// The [`grid-template-areas`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-template-areas) CSS property specifies named grid areas, establishing the cells in the grid and assigning them names.
pub static grid_template_areas: () = ();
/// The [`grid-template-columns`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-template-columns) CSS property defines the line names and track sizing functions of the grid columns.
pub static grid_template_columns: () = ();
/// The [`grid-template-rows`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/grid-template-rows) CSS property defines the line names and track sizing functions of the grid rows.
pub static grid_template_rows: () = ();
/// The [`hanging-punctuation`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/hanging-punctuation) CSS property specifies whether a punctuation mark should hang at the start or end of a line of text.
pub static hanging_punctuation: () = ();
/// The [`height`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/height) CSS property specifies the height of an element.
pub static height: () = ();
/// The [`hyphenate-character`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/hyphenate-character) CSS property sets the character (or string) used at the end of a line before a hyphenation break.
pub static hyphenate_character: () = ();
/// The [`hyphenate-limit-chars`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/hyphenate-limit-chars) CSS property specifies the minimum word length to allow hyphenation of words as well as the minimum number of characters before and after the hyphen.
pub static hyphenate_limit_chars: () = ();
/// The [`hyphens`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/hyphens) CSS property specifies how words should be hyphenated when text wraps across multiple lines.
pub static hyphens: () = ();
/// The [`image-orientation`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/image-orientation) CSS property specifies a layout-independent correction to the orientation of an image.
pub static image_orientation: () = ();
/// The [`image-rendering`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/image-rendering) CSS property sets an image scaling algorithm.
pub static image_rendering: () = ();
/// The [`initial-letter`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/initial-letter) CSS property sets the size and sink for dropped, raised, and sunken initial letters.
pub static initial_letter: () = ();
/// The [`inline-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/inline-size) CSS property defines the size of an element's block along the inline axis.
pub static inline_size: () = ();
/// The [`inset`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/inset) CSS property is a shorthand that corresponds to the `top`, `right`, `bottom`, and/or `left` properties.
pub static inset: () = ();
/// The [`inset-block`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/inset-block) CSS property defines the logical block start and end offsets of an element, which maps to physical offsets depending on the element's writing mode, directionality, and text orientation.
pub static inset_block: () = ();
/// The [`inset-block-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/inset-block-end) CSS property defines the logical block end offset of an element, which maps to a physical inset depending on the element's writing mode, directionality, and text orientation.
pub static inset_block_end: () = ();
/// The [`inset-block-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/inset-block-start) CSS property defines the logical block start offset of an element, which maps to a physical inset depending on the element's writing mode, directionality, and text orientation.
pub static inset_block_start: () = ();
/// The [`inset-inline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/inset-inline) CSS property defines the logical start and end offsets of an element in the inline direction, which maps to physical offsets depending on the element's writing mode, directionality, and text orientation.
pub static inset_inline: () = ();
/// The [`inset-inline-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/inset-inline-end) CSS property defines the logical inline end inset of an element, which maps to a physical offset depending on the element's writing mode, directionality, and text orientation.
pub static inset_inline_end: () = ();
/// The [`inset-inline-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/inset-inline-start) CSS property defines the logical inline start inset of an element, which maps to a physical offset depending on the element's writing mode, directionality, and text orientation.
pub static inset_inline_start: () = ();
/// The [`isolation`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/isolation) CSS property determines whether an element must create a new stacking context.
pub static isolation: () = ();
/// The CSS [`justify-content`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/justify-content) property defines how the browser distributes space between and around content items along the main axis of a flex container and the inline axis of grid and multicol containers.
pub static justify_content: () = ();
/// The CSS [`justify-items`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/justify-items) property defines the default `justify-self` for all items of the box, giving them all a default way of justifying each box along the appropriate axis.
pub static justify_items: () = ();
/// The CSS [`justify-self`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/justify-self) property sets the way a box is justified inside its alignment container along the appropriate axis.
pub static justify_self: () = ();
/// The [`left`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/left) CSS property participates in specifying the horizontal position of a positioned element.
pub static left: () = ();
/// The [`letter-spacing`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/letter-spacing) CSS property sets the spacing between text characters.
pub static letter_spacing: () = ();
/// The [`line-break`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/line-break) CSS property sets how to break lines of Chinese, Japanese, or Korean (CJK) text when working with punctuation and symbols.
pub static line_break: () = ();
/// The [`line-clamp`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/line-clamp) CSS property allows limiting of the contents of a block to the specified number of lines.
pub static line_clamp: () = ();
/// The [`line-height`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/line-height) CSS property sets the height of a line box in horizontal writing modes.
pub static line_height: () = ();
/// The [`list-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/list-style) CSS shorthand property allows you to set all the list style properties at once.
pub static list_style: () = ();
/// The [`list-style-image`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/list-style-image) CSS property sets an image to be used as the list item marker.
pub static list_style_image: () = ();
/// The [`list-style-position`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/list-style-position) CSS property sets the position of the `::marker` relative to a list item.
pub static list_style_position: () = ();
/// The [`list-style-type`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/list-style-type) CSS property sets the marker (such as a disc, character, or custom counter style) of a list item element.
pub static list_style_type: () = ();
/// The [`margin`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin) CSS shorthand property sets the margin area on all four sides of an element.
pub static margin: () = ();
/// The [`margin-block`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-block) CSS shorthand property defines the logical block start and end margins of an element, which maps to physical margins depending on the element's writing mode, directionality, and text orientation.
pub static margin_block: () = ();
/// The [`margin-block-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-block-end) CSS property defines the logical block end margin of an element, which maps to a physical margin depending on the element's writing mode, directionality, and text orientation.
pub static margin_block_end: () = ();
/// The [`margin-block-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-block-start) CSS property defines the logical block start margin of an element, which maps to a physical margin depending on the element's writing mode, directionality, and text orientation.
pub static margin_block_start: () = ();
/// The [`margin-bottom`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-bottom) CSS property sets the margin area on the bottom of an element.
pub static margin_bottom: () = ();
/// The [`margin-inline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-inline) CSS shorthand property defines both the logical inline start and end margins of an element, which maps to physical margins depending on the element's writing mode, directionality, and text orientation.
pub static margin_inline: () = ();
/// The [`margin-inline-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-inline-end) CSS property defines the logical inline end margin of an element, which maps to a physical margin depending on the element's writing mode, directionality, and text orientation.
pub static margin_inline_end: () = ();
/// The [`margin-inline-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-inline-start) CSS property defines the logical inline start margin of an element, which maps to a physical margin depending on the element's writing mode, directionality, and text orientation.
pub static margin_inline_start: () = ();
/// The [`margin-left`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-left) CSS property sets the margin area on the left side of an element.
pub static margin_left: () = ();
/// The [`margin-right`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-right) CSS property sets the margin area on the right side of an element.
pub static margin_right: () = ();
/// The [`margin-top`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/margin-top) CSS property sets the margin area on the top of an element.
pub static margin_top: () = ();
/// The [`marker`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/marker) CSS property points to a marker that will be drawn on the first, middle, and last vertices of the element's path; that is, at all of its vertices.
pub static marker: () = ();
/// The [`marker-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/marker-end) CSS property points to a marker that will be drawn on the last vertex of the element's path; that is, at its ending vertex.
pub static marker_end: () = ();
/// The [`marker-mid`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/marker-mid) CSS property points to a marker that will be drawn on the middle vertices of the element's path; that is, at each of its vertices between the start and end vertices.
pub static marker_mid: () = ();
/// The [`marker-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/marker-start) CSS property points to a marker that will be drawn on the first vertex of the element's path; that is, at its starting vertex.
pub static marker_start: () = ();
/// The [`mask`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask) CSS shorthand property hides an element (partially or fully) by masking or clipping a specified area of the image.
pub static mask: () = ();
/// The [`mask-border`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-border) CSS shorthand property lets you create a mask along the edge of an element's border.
pub static mask_border: () = ();
/// The [`mask-border-mode`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-border-mode) CSS property specifies the blending mode used in a mask border.
pub static mask_border_mode: () = ();
/// The [`mask-border-outset`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-border-outset) CSS property specifies the distance by which an element's mask border is set out from its border box.
pub static mask_border_outset: () = ();
/// The [`mask-border-repeat`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-border-repeat) CSS property specifies how the images for the sides and the middle part of the mask border image are scaled and tiled.
pub static mask_border_repeat: () = ();
/// The [`mask-border-slice`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-border-slice) CSS property divides the image set by `mask-border-source` into regions.
pub static mask_border_slice: () = ();
/// The [`mask-border-source`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-border-source) CSS property sets the source image used to create an element's mask border.
pub static mask_border_source: () = ();
/// The [`mask-border-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-border-width) CSS property sets the width of an element's mask border.
pub static mask_border_width: () = ();
/// The [`mask-clip`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-clip) CSS property determines the area which is affected by a mask.
pub static mask_clip: () = ();
/// The [`mask-composite`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-composite) CSS property represents a compositing operation used on the current mask layer with the mask layers below it.
pub static mask_composite: () = ();
/// The [`mask-image`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-image) CSS property sets the image that is used as the mask layer for an element, hiding sections of the element on which the masking image is set based on the alpha channel of the mask image and, depending on the `mask-mode` property value, the luminance of the mask image's colors.
pub static mask_image: () = ();
/// The [`mask-mode`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-mode) CSS property is set on the element being masked.
pub static mask_mode: () = ();
/// The [`mask-origin`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-origin) CSS property sets the origin of a mask.
pub static mask_origin: () = ();
/// The [`mask-position`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-position) CSS property sets the initial position, relative to the mask position layer set by `mask-origin`, for each defined mask image.
pub static mask_position: () = ();
/// The [`mask-repeat`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-repeat) CSS property sets how mask images are repeated.
pub static mask_repeat: () = ();
/// The [`mask-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mask-size) CSS property specifies the sizes of specified mask images.
pub static mask_size: () = ();
/// The [`math-depth`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/math-depth) property describes a notion of depth for each element of a mathematical formula, with respect to the top-level container of that formula.
pub static math_depth: () = ();
/// The [`math-shift`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/math-shift) property indicates whether superscripts inside MathML formulas should be raised by a normal or compact shift.
pub static math_shift: () = ();
/// The [`math-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/math-style) property indicates whether MathML equations should render with normal or compact height.
pub static math_style: () = ();
/// The [`max-block-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/max-block-size) CSS property specifies the maximum size of an element in the direction opposite that of the writing direction as specified by `writing-mode`.
pub static max_block_size: () = ();
/// The [`max-height`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/max-height) CSS property sets the maximum height of an element.
pub static max_height: () = ();
/// The [`max-inline-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/max-inline-size) CSS property defines the horizontal or vertical maximum size of an element's block, depending on its writing mode.
pub static max_inline_size: () = ();
/// The [`max-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/max-width) CSS property sets the maximum width of an element.
pub static max_width: () = ();
/// The [`min-block-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/min-block-size) CSS property defines the minimum horizontal or vertical size of an element's block, depending on its writing mode.
pub static min_block_size: () = ();
/// The [`min-height`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/min-height) CSS property sets the minimum height of an element.
pub static min_height: () = ();
/// The [`min-inline-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/min-inline-size) CSS property defines the horizontal or vertical minimal size of an element's block, depending on its writing mode.
pub static min_inline_size: () = ();
/// The [`min-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/min-width) CSS property sets the minimum width of an element.
pub static min_width: () = ();
/// The [`mix-blend-mode`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/mix-blend-mode) CSS property sets how an element's content should blend with the content of the element's parent and the element's background.
pub static mix_blend_mode: () = ();
/// The [`object-fit`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/object-fit) CSS property sets how the content of a replaced element, such as an `<img>` or `<video>`, should be resized to fit its container.
pub static object_fit: () = ();
/// The [`object-position`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/object-position) CSS property specifies the alignment of the selected replaced element's contents within the element's box.
pub static object_position: () = ();
/// The [`offset`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/offset) CSS shorthand property sets all the properties required for animating an element along a defined path.
pub static offset: () = ();
/// The [`offset-anchor`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/offset-anchor) CSS property specifies the point inside the box of an element traveling along an `offset-path` that is actually moving along the path.
pub static offset_anchor: () = ();
/// The [`offset-distance`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/offset-distance) CSS property specifies a position along an `offset-path` for an element to be placed.
pub static offset_distance: () = ();
/// The [`offset-position`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/offset-position) CSS property defines the initial position of an element along a path.
pub static offset_position: () = ();
/// The [`offset-rotate`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/offset-rotate) CSS property defines the orientation/direction of the element as it is positioned along the `offset-path`.
pub static offset_rotate: () = ();
/// The [`opacity`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/opacity) CSS property sets the opacity of an element.
pub static opacity: () = ();
/// The [`order`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/order) CSS property sets the order to lay out an item in a flex or grid container.
pub static order: () = ();
/// The [`orphans`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/orphans) CSS property sets the minimum number of lines in a block container that must be shown at the bottom of a page, region, or column.
pub static orphans: () = ();
/// The [`outline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/outline) CSS shorthand property sets most of the outline properties in a single declaration.
pub static outline: () = ();
/// The [`outline-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/outline-color) CSS property sets the color of an element's outline.
pub static outline_color: () = ();
/// The [`outline-offset`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/outline-offset) CSS property sets the amount of space between an outline and the edge or border of an element.
pub static outline_offset: () = ();
/// The [`outline-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/outline-style) CSS property sets the style of an element's outline.
pub static outline_style: () = ();
/// The CSS [`outline-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/outline-width) property sets the thickness of an element's outline.
pub static outline_width: () = ();
/// The [`overflow`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overflow) CSS shorthand property sets the desired behavior when content does not fit in the element's padding box (overflows) in the horizontal and/or vertical direction.
pub static overflow: () = ();
/// The [`overflow-anchor`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overflow-anchor) CSS property provides a way to opt out of the browser's scroll anchoring behavior, which adjusts scroll position to minimize content shifts.
pub static overflow_anchor: () = ();
/// The [`overflow-block`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overflow-block) CSS property sets what shows when content overflows the block start and block end edges of a box.
pub static overflow_block: () = ();
/// The [`overflow-clip-margin`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overflow-clip-margin) CSS property determines how far outside its bounds an element with `overflow: clip` may be painted before being clipped.
pub static overflow_clip_margin: () = ();
/// The [`overflow-inline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overflow-inline) CSS property sets what shows when content overflows the inline start and end edges of a box.
pub static overflow_inline: () = ();
/// The [`overflow-wrap`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overflow-wrap) CSS property applies to text, setting whether the browser should insert line breaks within an otherwise unbreakable string to prevent text from overflowing its line box.
pub static overflow_wrap: () = ();
/// The [`overflow-x`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overflow-x) CSS property sets what shows when content overflows a block-level element's left and right edges.
pub static overflow_x: () = ();
/// The [`overflow-y`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overflow-y) CSS property sets what shows when content overflows a block-level element's top and bottom edges.
pub static overflow_y: () = ();
/// The [`overscroll-behavior`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overscroll-behavior) CSS property sets what a browser does when reaching the boundary of a scrolling area.
pub static overscroll_behavior: () = ();
/// The [`overscroll-behavior-block`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overscroll-behavior-block) CSS property sets the browser's behavior when the block direction boundary of a scrolling area is reached.
pub static overscroll_behavior_block: () = ();
/// The [`overscroll-behavior-inline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overscroll-behavior-inline) CSS property sets the browser's behavior when the inline direction boundary of a scrolling area is reached.
pub static overscroll_behavior_inline: () = ();
/// The [`overscroll-behavior-x`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overscroll-behavior-x) CSS property sets the browser's behavior when the horizontal boundary of a scrolling area is reached.
pub static overscroll_behavior_x: () = ();
/// The [`overscroll-behavior-y`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/overscroll-behavior-y) CSS property sets the browser's behavior when the vertical boundary of a scrolling area is reached.
pub static overscroll_behavior_y: () = ();
/// The [`padding`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding) CSS shorthand property sets the padding area on all four sides of an element at once.
pub static padding: () = ();
/// The [`padding-block`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding-block) CSS shorthand property defines the logical block start and end padding of an element, which maps to physical padding properties depending on the element's writing mode, directionality, and text orientation.
pub static padding_block: () = ();
/// The [`padding-block-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding-block-end) CSS property defines the logical block end padding of an element, which maps to a physical padding depending on the element's writing mode, directionality, and text orientation.
pub static padding_block_end: () = ();
/// The [`padding-block-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding-block-start) CSS property defines the logical block start padding of an element, which maps to a physical padding depending on the element's writing mode, directionality, and text orientation.
pub static padding_block_start: () = ();
/// The [`padding-bottom`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding-bottom) CSS property sets the height of the padding area on the bottom of an element.
pub static padding_bottom: () = ();
/// The [`padding-inline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding-inline) CSS shorthand property defines the logical inline start and end padding of an element, which maps to physical padding properties depending on the element's writing mode, directionality, and text orientation.
pub static padding_inline: () = ();
/// The [`padding-inline-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding-inline-end) CSS property defines the logical inline end padding of an element, which maps to a physical padding depending on the element's writing mode, directionality, and text orientation.
pub static padding_inline_end: () = ();
/// The [`padding-inline-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding-inline-start) CSS property defines the logical inline start padding of an element, which maps to a physical padding depending on the element's writing mode, directionality, and text orientation.
pub static padding_inline_start: () = ();
/// The [`padding-left`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding-left) CSS property sets the width of the padding area to the left of an element.
pub static padding_left: () = ();
/// The [`padding-right`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding-right) CSS property sets the width of the padding area on the right of an element.
pub static padding_right: () = ();
/// The [`padding-top`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/padding-top) CSS property sets the height of the padding area on the top of an element.
pub static padding_top: () = ();
/// The [`page`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/page) CSS property is used to specify the named page, a specific type of page defined by the `@page` at-rule.
pub static page: () = ();
/// The [`paint-order`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/paint-order) CSS property lets you control the order in which the fill and stroke (and painting markers) of text content and shapes are drawn.
pub static paint_order: () = ();
/// The [`perspective`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/perspective) CSS property determines the distance between the z=0 plane and the user in order to give a 3D-positioned element some perspective.
pub static perspective: () = ();
/// The [`perspective-origin`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/perspective-origin) CSS property determines the position at which the viewer is looking.
pub static perspective_origin: () = ();
/// The [`place-content`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/place-content) CSS shorthand property allows you to align content along both the block and inline directions at once (i.e., the `align-content` and `justify-content` properties) in a relevant layout system such as Grid or Flexbox.
pub static place_content: () = ();
/// The CSS [`place-items`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/place-items) shorthand property aligns items along both the block and inline directions at once.
pub static place_items: () = ();
/// The [`place-self`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/place-self) CSS shorthand property allows you to align an individual item in both the block and inline directions at once (i.e., the `align-self` and `justify-self` properties).
pub static place_self: () = ();
/// The [`pointer-events`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/pointer-events) CSS property sets under what circumstances (if any) a particular graphic element can become the target of pointer events.
pub static pointer_events: () = ();
/// The [`position`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/position) CSS property sets how an element is positioned in a document.
pub static position: () = ();
/// The [`position-anchor`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/position-anchor) CSS property specifies the default anchor element for a positioned element.
pub static position_anchor: () = ();
/// The [`position-area`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/position-area) CSS property enables an anchor-positioned element to be positioned relative to the edges of its associated anchor element by placing the positioned element on one or more tiles of an implicit 3x3 grid, where the anchoring element is the center cell.
pub static position_area: () = ();
/// The [`position-try`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/position-try) CSS property is a shorthand that corresponds to the `position-try-order` and `position-try-fallbacks` properties.
pub static position_try: () = ();
/// The [`position-try-fallbacks`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/position-try-fallbacks) CSS property enables you to specify a list of one or more alternative position try fallback options for anchor-positioned elements to be placed relative to their associated anchor elements.
pub static position_try_fallbacks: () = ();
/// The [`position-try-order`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/position-try-order) CSS property allows you to specify various fallback options that result in an available position-try fallback being used to set an anchor-positioned element's position, instead of its initial position settings.
pub static position_try_order: () = ();
/// The [`position-visibility`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/position-visibility) CSS property enables conditionally hiding an anchor-positioned element depending on, for example, whether it is overflowing its containing element or the viewport.
pub static position_visibility: () = ();
/// The [`print-color-adjust`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/print-color-adjust) CSS property sets what, if anything, the user agent may do to optimize the appearance of the element on the output device.
pub static print_color_adjust: () = ();
/// The CSS [`quotes`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/quotes) property sets how the browser should render quotation marks that are automatically added to the HTML `<q>` element or added using the `open-quotes` or `close-quotes` (or omitted using the `no-open-quote` and `no-close-quote`) values of the of the CSS `content` property.
pub static quotes: () = ();
/// The [`r`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/r) CSS property defines the radius of a circle.
pub static r: () = ();
/// The [`resize`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/resize) CSS property sets whether an element is resizable, and if so, in which directions.
pub static resize: () = ();
/// The [`right`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/right) CSS property participates in specifying the horizontal position of a positioned element.
pub static right: () = ();
/// The [`rotate`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/rotate) CSS property allows you to specify rotation transforms individually and independently of the `transform` property.
pub static rotate: () = ();
/// The [`row-gap`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/row-gap) CSS property sets the size of the gap (gutter) between an element's rows.
pub static row_gap: () = ();
/// The [`ruby-align`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/ruby-align) CSS property defines the distribution of the different ruby elements over the base.
pub static ruby_align: () = ();
/// The [`ruby-overhang`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/ruby-overhang) CSS property specifies whether or not a `<ruby>` annotation overhangs any surrounding text.
pub static ruby_overhang: () = ();
/// The [`ruby-position`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/ruby-position) CSS property defines the position of a ruby element relative to its base element.
pub static ruby_position: () = ();
/// The [`scale`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scale) CSS property allows you to specify scale transforms individually and independently of the `transform` property.
pub static scale: () = ();
/// The [`scroll-behavior`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-behavior) CSS property sets the behavior for a scrolling box when scrolling is triggered by the navigation or CSSOM scrolling APIs.
pub static scroll_behavior: () = ();
/// The [`scroll-margin`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin) shorthand property sets all of the scroll margins of an element at once, assigning values much like the `margin` property does for margins of an element.
pub static scroll_margin: () = ();
/// The [`scroll-margin-block`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin-block) shorthand property sets the scroll margins of an element in the block dimension.
pub static scroll_margin_block: () = ();
/// The [`scroll-margin-block-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin-block-end) property defines the margin of the scroll snap area at the end of the block dimension that is used for snapping this box to the snapport.
pub static scroll_margin_block_end: () = ();
/// The [`scroll-margin-block-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin-block-start) property defines the margin of the scroll snap area at the start of the block dimension that is used for snapping this box to the snapport.
pub static scroll_margin_block_start: () = ();
/// The [`scroll-margin-bottom`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin-bottom) property defines the bottom margin of the scroll snap area that is used for snapping this box to the snapport.
pub static scroll_margin_bottom: () = ();
/// The [`scroll-margin-inline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin-inline) shorthand property sets the scroll margins of an element in the inline dimension.
pub static scroll_margin_inline: () = ();
/// The [`scroll-margin-inline-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin-inline-end) property defines the margin of the scroll snap area at the end of the inline dimension that is used for snapping this box to the snapport.
pub static scroll_margin_inline_end: () = ();
/// The [`scroll-margin-inline-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin-inline-start) property defines the margin of the scroll snap area at the start of the inline dimension that is used for snapping this box to the snapport.
pub static scroll_margin_inline_start: () = ();
/// The [`scroll-margin-left`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin-left) property defines the left margin of the scroll snap area that is used for snapping this box to the snapport.
pub static scroll_margin_left: () = ();
/// The [`scroll-margin-right`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin-right) property defines the right margin of the scroll snap area that is used for snapping this box to the snapport.
pub static scroll_margin_right: () = ();
/// The [`scroll-margin-top`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-margin-top) property defines the top margin of the scroll snap area that is used for snapping this box to the snapport.
pub static scroll_margin_top: () = ();
/// The [`scroll-padding`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding) shorthand property sets scroll padding on all sides of an element at once.
pub static scroll_padding: () = ();
/// The [`scroll-padding-block`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding-block) shorthand property sets the scroll padding of an element in the block dimension.
pub static scroll_padding_block: () = ();
/// The [`scroll-padding-block-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding-block-end) property defines offsets for the end edge in the block dimension of the optimal viewing region of the scrollport: the region used as the target region for placing things in view of the user.
pub static scroll_padding_block_end: () = ();
/// The [`scroll-padding-block-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding-block-start) property defines offsets for the start edge in the block dimension of the optimal viewing region of the scrollport: the region used as the target region for placing things in view of the user.
pub static scroll_padding_block_start: () = ();
/// The [`scroll-padding-bottom`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding-bottom) property defines offsets for the bottom of the optimal viewing region of the scrollport: the region used as the target region for placing things in view of the user.
pub static scroll_padding_bottom: () = ();
/// The [`scroll-padding-inline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding-inline) shorthand property sets the scroll padding of an element in the inline dimension.
pub static scroll_padding_inline: () = ();
/// The [`scroll-padding-inline-end`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding-inline-end) property defines offsets for the end edge in the inline dimension of the optimal viewing region of the scrollport: the region used as the target region for placing things in view of the user.
pub static scroll_padding_inline_end: () = ();
/// The [`scroll-padding-inline-start`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding-inline-start) property defines offsets for the start edge in the inline dimension of the optimal viewing region of the scrollport: the region used as the target region for placing things in view of the user.
pub static scroll_padding_inline_start: () = ();
/// The [`scroll-padding-left`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding-left) property defines offsets for the left of the optimal viewing region of the scrollport: the region used as the target region for placing things in view of the user.
pub static scroll_padding_left: () = ();
/// The [`scroll-padding-right`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding-right) property defines offsets for the right of the optimal viewing region of the scrollport: the region used as the target region for placing things in view of the user.
pub static scroll_padding_right: () = ();
/// The [`scroll-padding-top`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-padding-top) property defines offsets for the top of the optimal viewing region of the scrollport: the region used as the target region for placing things in view of the user.
pub static scroll_padding_top: () = ();
/// The [`scroll-snap-align`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-snap-align) property specifies the box's snap position as an alignment of its snap area (as the alignment subject) within its snap container's snap port (as the alignment container).
pub static scroll_snap_align: () = ();
/// The [`scroll-snap-stop`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-snap-stop) CSS property defines whether or not the scroll container is allowed to "pass over" possible snap positions.
pub static scroll_snap_stop: () = ();
/// The [`scroll-snap-type`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-snap-type) CSS property is set on a scroll container, opting it into scroll snapping by setting the direction and strictness of snap point enforcement within the snap port.
pub static scroll_snap_type: () = ();
/// The [`scroll-timeline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-timeline) CSS shorthand property is used to define a named scroll progress timeline, which is progressed through by scrolling a scrollable element (scroller) between top and bottom (or left and right).
pub static scroll_timeline: () = ();
/// The [`scroll-timeline-axis`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-timeline-axis) CSS property is used to specify the scrollbar direction that will be used to provide the timeline for a scroll driven animation, which is progressed through by scrolling a scrollable element (scroller).
pub static scroll_timeline_axis: () = ();
/// The [`scroll-timeline-name`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scroll-timeline-name) CSS property is used to define the name of a named scroll progress timeline, which is progressed through by scrolling a scrollable element (scroller) between top and bottom (or left and right).
pub static scroll_timeline_name: () = ();
/// The [`scrollbar-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scrollbar-color) CSS property sets the color of the scrollbar track and thumb.
pub static scrollbar_color: () = ();
/// The [`scrollbar-gutter`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scrollbar-gutter) CSS property allows authors to reserve space for the scrollbar, preventing unwanted layout changes as the content grows while also avoiding unnecessary visuals when scrolling isn't needed.
pub static scrollbar_gutter: () = ();
/// The [`scrollbar-width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/scrollbar-width) property allows the author to set the desired thickness of an element's scrollbars when they are shown.
pub static scrollbar_width: () = ();
/// The [`shape-image-threshold`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/shape-image-threshold) CSS property sets the alpha channel threshold used to extract the shape using an image as the value for `shape-outside`.
pub static shape_image_threshold: () = ();
/// The [`shape-margin`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/shape-margin) CSS property sets a margin for a CSS shape created using `shape-outside`.
pub static shape_margin: () = ();
/// The [`shape-outside`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/shape-outside) CSS property defines a shape—which may be non-rectangular—around which adjacent inline content should wrap.
pub static shape_outside: () = ();
/// The [`shape-rendering`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/shape-rendering) CSS property provides hints to the renderer about what tradeoffs to make when rendering shapes like paths, circles, or rectangles.
pub static shape_rendering: () = ();
/// The [`tab-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/tab-size) CSS property is used to customize the width of tab characters (U+0009).
pub static tab_size: () = ();
/// The [`table-layout`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/table-layout) CSS property sets the algorithm used to lay out `<table>` cells, rows, and columns.
pub static table_layout: () = ();
/// The [`text-align`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-align) CSS property sets the horizontal alignment of the inline-level content inside a block element or table-cell box.
pub static text_align: () = ();
/// The [`text-align-last`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-align-last) CSS property sets how the last line of a block or a line, right before a forced line break, is aligned.
pub static text_align_last: () = ();
/// The [`text-anchor`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-anchor) CSS property aligns a box containing a string of text where the wrapping area is determined from the `inline-size` property, and the text is then placed relative to the anchor point of the element, which is defined using the `x` and `y` (or `dx` and `dy`) attributes.
pub static text_anchor: () = ();
/// The [`text-autospace`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-autospace) CSS property allows you to specify the space applied between Chinese/Japanese/Korean (CJK) and non-CJK characters.
pub static text_autospace: () = ();
/// The [`text-box`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-box) CSS property is a shorthand that corresponds to the `text-box-trim` and `text-box-edge` properties, which together specify the amount of space to trim from the block-start edge and block-end edge of a text element's block container.
pub static text_box: () = ();
/// The [`text-box-edge`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-box-edge) CSS property specifies an amount of space to trim from a text element's block container.
pub static text_box_edge: () = ();
/// The [`text-box-trim`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-box-trim) CSS property specifies which of the over and under edges of text content to trim from a text element's block container.
pub static text_box_trim: () = ();
/// The [`text-combine-upright`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-combine-upright) CSS property sets the combination of characters into the space of a single character.
pub static text_combine_upright: () = ();
/// The [`text-decoration`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-decoration) shorthand CSS property sets the appearance of decorative lines on text.
pub static text_decoration: () = ();
/// The [`text-decoration-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-decoration-color) CSS property sets the color of decorations added to text by `text-decoration-line`.
pub static text_decoration_color: () = ();
/// The [`text-decoration-line`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-decoration-line) CSS property sets the kind of decoration that is used on text in an element, such as an underline or overline.
pub static text_decoration_line: () = ();
/// The [`text-decoration-skip-ink`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-decoration-skip-ink) CSS property specifies how overlines and underlines are drawn when they pass over glyph ascenders and descenders.
pub static text_decoration_skip_ink: () = ();
/// The [`text-decoration-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-decoration-style) CSS property sets the style of the lines specified by `text-decoration-line`.
pub static text_decoration_style: () = ();
/// The [`text-decoration-thickness`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-decoration-thickness) CSS property sets the stroke thickness of the decoration line that is used on text in an element, such as a line-through, underline, or overline.
pub static text_decoration_thickness: () = ();
/// The [`text-emphasis`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-emphasis) CSS property applies emphasis marks to text (except spaces and control characters).
pub static text_emphasis: () = ();
/// The [`text-emphasis-color`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-emphasis-color) CSS property sets the color of emphasis marks.
pub static text_emphasis_color: () = ();
/// The [`text-emphasis-position`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-emphasis-position) CSS property sets where emphasis marks are drawn.
pub static text_emphasis_position: () = ();
/// The [`text-emphasis-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-emphasis-style) CSS property sets the appearance of emphasis marks.
pub static text_emphasis_style: () = ();
/// The [`text-indent`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-indent) CSS property sets the length of empty space (indentation) that is put before lines of text in a block.
pub static text_indent: () = ();
/// The [`text-justify`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-justify) CSS property sets what type of justification should be applied to text when `text-align: justify;` is set on an element.
pub static text_justify: () = ();
/// The [`text-orientation`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-orientation) CSS property sets the orientation of the text characters in a line.
pub static text_orientation: () = ();
/// The [`text-overflow`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-overflow) CSS property sets how hidden overflow content is signaled to users.
pub static text_overflow: () = ();
/// The [`text-rendering`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-rendering) CSS property provides information to the rendering engine about what to optimize for when rendering text.
pub static text_rendering: () = ();
/// The [`text-shadow`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-shadow) CSS property adds shadows to text.
pub static text_shadow: () = ();
/// The [`text-transform`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-transform) CSS property specifies how to capitalize an element's text.
pub static text_transform: () = ();
/// The [`text-underline-offset`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-underline-offset) CSS property sets the offset distance of an underline text decoration line (applied using `text-decoration`) from its original position.
pub static text_underline_offset: () = ();
/// The [`text-underline-position`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-underline-position) CSS property specifies the position of the underline which is set using the `text-decoration` property's `underline` value.
pub static text_underline_position: () = ();
/// The [`text-wrap`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-wrap) CSS shorthand property controls how text inside an element is wrapped.
pub static text_wrap: () = ();
/// The [`text-wrap-mode`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-wrap-mode) CSS property controls whether the text inside an element is wrapped.
pub static text_wrap_mode: () = ();
/// The [`text-wrap-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/text-wrap-style) CSS property controls how text inside an element is wrapped.
pub static text_wrap_style: () = ();
/// The [`timeline-scope`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/timeline-scope) CSS property modifies the scope of a named animation timeline.
pub static timeline_scope: () = ();
/// The [`top`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/top) CSS property sets the vertical position of a positioned element.
pub static top: () = ();
/// The [`touch-action`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/touch-action) CSS property sets how an element's region can be manipulated by a touchscreen user (for example, by zooming features built into the browser).
pub static touch_action: () = ();
/// The [`transform`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transform) CSS property lets you rotate, scale, skew, or translate an element.
pub static transform: () = ();
/// The [`transform-box`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transform-box) CSS property defines the layout box to which the `transform`, individual transform properties `translate`, `scale`, and `rotate`, and `transform-origin` properties relate.
pub static transform_box: () = ();
/// The [`transform-origin`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transform-origin) CSS property sets the origin for an element's transformations.
pub static transform_origin: () = ();
/// The [`transform-style`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transform-style) CSS property sets whether children of an element are positioned in the 3D space or are flattened in the plane of the element.
pub static transform_style: () = ();
/// The [`transition`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transition) CSS property is a shorthand property for `transition-property`, `transition-duration`, `transition-timing-function`, `transition-delay`, and `transition-behavior`.
pub static transition: () = ();
/// The [`transition-behavior`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transition-behavior) CSS property specifies whether transitions will be started for properties whose animation behavior is discrete.
pub static transition_behavior: () = ();
/// The [`transition-delay`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transition-delay) CSS property specifies the duration to wait before starting a property's transition effect when its value changes.
pub static transition_delay: () = ();
/// The [`transition-duration`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transition-duration) CSS property sets the length of time a transition animation should take to complete.
pub static transition_duration: () = ();
/// The [`transition-property`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transition-property) CSS property sets the CSS properties to which a transition effect should be applied.
pub static transition_property: () = ();
/// The [`transition-timing-function`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transition-timing-function) CSS property sets how intermediate values are calculated for CSS properties being affected by a transition effect.
pub static transition_timing_function: () = ();
/// The [`translate`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/translate) CSS property allows you to specify translation transforms individually and independently of the `transform` property.
pub static translate: () = ();
/// The [`unicode-bidi`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/unicode-bidi) CSS property, together with the `direction` property, determines how bidirectional text in a document is handled.
pub static unicode_bidi: () = ();
/// The [`user-select`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/user-select) CSS property controls whether the user can select text.
pub static user_select: () = ();
/// The [`vertical-align`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/vertical-align) CSS shorthand property sets the vertical alignment of an inline, inline-block, or table-cell box.
pub static vertical_align: () = ();
/// The [`view-timeline`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/view-timeline) CSS shorthand property defines a named view progress timeline's name, direction, and inset values.
pub static view_timeline: () = ();
/// The [`view-timeline-axis`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/view-timeline-axis) CSS property specifies the scroll direction to be used for a named view progress timeline.
pub static view_timeline_axis: () = ();
/// The [`view-timeline-inset`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/view-timeline-inset) CSS property is used to specify one or two values representing an adjustment to the position of the scrollport (see Scroll container for more details) in which the subject element of a named view progress timeline animation is deemed to be visible.
pub static view_timeline_inset: () = ();
/// The [`view-timeline-name`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/view-timeline-name) CSS property specifies the names of one or more named view progress timelines associated with the element.
pub static view_timeline_name: () = ();
/// The [`view-transition-class`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/view-transition-class) CSS property provides the selected elements with one or more identifying classes (`<custom-ident>`s), providing an additional method of styling the view transitions for those elements.
pub static view_transition_class: () = ();
/// The [`view-transition-name`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/view-transition-name) CSS property specifies the view transition snapshot that selected elements will participate in.
pub static view_transition_name: () = ();
/// The [`visibility`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/visibility) CSS property shows or hides an element without changing the layout of a document.
pub static visibility: () = ();
/// The [`white-space`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/white-space) CSS property sets how white space inside an element is handled.
pub static white_space: () = ();
/// The [`white-space-collapse`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/white-space-collapse) CSS property controls how white space inside an element is collapsed.
pub static white_space_collapse: () = ();
/// The [`widows`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/widows) CSS property sets the minimum number of lines in a block container that must be shown at the top of a page, region, or column.
pub static widows: () = ();
/// The [`width`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/width) CSS property sets an element's width.
pub static width: () = ();
/// The [`will-change`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/will-change) CSS property hints to browsers how an element is expected to change.
pub static will_change: () = ();
/// The [`word-break`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/word-break) CSS property sets whether line breaks appear wherever the text would otherwise overflow its content box.
pub static word_break: () = ();
/// The [`word-spacing`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/word-spacing) CSS property sets the spacing between words and between tags.
pub static word_spacing: () = ();
/// The [`writing-mode`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/writing-mode) CSS property sets whether lines of text are laid out horizontally or vertically, as well as the direction in which blocks progress.
pub static writing_mode: () = ();
/// The [`z-index`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/z-index) CSS property sets the z-order of a positioned element and its descendants or flex and grid items.
pub static z_index: () = ();
/// The [`zoom`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/zoom) CSS property can be used to control the magnification level of an element.
pub static zoom: () = ();
